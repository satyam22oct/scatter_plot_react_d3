import initState from './init_state';

// experiment reducer
export default function(state=initState, action = {}) {
  let newState = state;
  switch(action.type) {

		default:
		break;
  }
	return newState;
}
